{
  "name": "feishu-file-export",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "飞书多维表格边栏插件：按单元格内容命名、分类批量导出附件",
  "scripts": {
    "dev": "vite --host 0.0.0.0",
    "build": "tsc --noEmit && vite build",
    "test": "node --experimental-strip-types --test tests/*.test.ts",
    "preview": "vite preview --host 0.0.0.0"
  },
  "engines": { "node": ">=24" },
  "dependencies": {
    "@lark-base-open/js-sdk": "1.0.2",
    "jszip": "3.10.1"
  },
  "devDependencies": {
    "typescript": "5.9.3",
    "vite": "8.3.0"
  }
}
